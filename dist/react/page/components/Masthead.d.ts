export interface MastheadProps {
    title: string;
    group?: string;
    resource?: string;
    total?: number;
}
export declare function Masthead({ title, group, resource, total }: MastheadProps): import("react").JSX.Element;
